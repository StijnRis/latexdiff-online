{
  "name": "latexdiff-online",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Run latexdiff in the browser with WebPerl / WebAssembly. Compare two LaTeX files without installing Perl.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "download-assets": "node scripts/download-assets.mjs",
    "postinstall": "node scripts/download-assets.mjs"
  },
  "devDependencies": {
    "coi-serviceworker": "^0.1.7",
    "jsdom": "^30.0.1",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
